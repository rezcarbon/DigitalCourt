import Foundation
import SwiftData
import Combine

// MARK: - This file contains legacy model definitions that are now handled elsewhere
// The primary DBrain model is in AppDataModels.swift
// The EnhancedBootSequenceData struct is in BrainLoader.swift

// This file can be used for other shared model types that don't conflict with existing definitions